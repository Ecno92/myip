sudo: false
language: python
install:
  - pip install pipenv --upgrade
cache: pip
matrix:
  include:
    - python: 3.6
  include:
    - python: 3.7
      dist: xenial
      sudo: true
script:
  - make flake8
  - make mypy
  - make pytest
